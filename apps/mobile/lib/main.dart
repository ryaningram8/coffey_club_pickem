import 'package:coffey_ui/coffey_ui.dart';
import 'package:flutter/material.dart';

void main() {
  WidgetsFlutterBinding.ensureInitialized();
  // Firebase initialized in Phase 4 (push notifications)
  runApp(const CoffeyApp());
}
